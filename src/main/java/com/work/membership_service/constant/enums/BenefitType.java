package com.work.membership_service.constant.enums;

// supported benefit kinds, each backed by a strategy bean
public enum BenefitType {
    FREE_DELIVERY,
    EXTRA_DISCOUNT,
    EXCLUSIVE_DEALS,
    EARLY_ACCESS,
    PRIORITY_SUPPORT
}
